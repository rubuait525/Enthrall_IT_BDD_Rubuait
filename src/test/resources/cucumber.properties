cucumber.features 	 	       		= src/test/java/feature
cucumber.glue						= stepDefinition, baseUtil
cucumber.filter.tags				= @dashboard or @login
cucumber.plugin 					= pretty, html:target/report.html, json:target/report.json, junit:target/report.xml
cucumber.execution.dry-run	= false
cucumber.snippet-type			= camelcase

# This properties is used to control the feature file
# if the tags are absent here, then run from the feature file 
# if line five is false, then you can run from feature file, if true, no run from feature file